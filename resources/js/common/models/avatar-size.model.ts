// This is strongly typed so we don't wind up with 100 different possible sizes.
// If possible, use one of these sane defaults. Only add another one if necessary.
export type AvatarSize = 8 | 16 | 24 | 32 | 48 | 64 | 128;
