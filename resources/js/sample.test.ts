import { describe, it, expect } from 'vitest';

// TODO: Get rid of this sample once we have some real tests.
// This is purely for reference and to confirm Vitest is operational.
describe('Vitest Sample', () => {
  it('can run some tests', () => {
    expect(2 + 2).toEqual(4);
  });
});
